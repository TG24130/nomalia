/**
 * tourisme.js — tiroir « Type de tourisme ».
 *
 * Lit et écrit l'objet voyage via js/voyage.js uniquement (CLAUDE.md §3.3).
 * À implémenter à l'étape 9 du plan V0.
 */

/**
 * Affiche le tiroir dans le conteneur fourni.
 * @param {HTMLElement} conteneur
 * @param {object} voyage
 */
export function afficher(conteneur, voyage) {
  throw new Error('tourisme.js : à implémenter (étape 9 du plan V0)');
}
